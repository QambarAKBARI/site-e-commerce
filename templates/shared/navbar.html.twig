<nav class="navbar navbar-expand-lg navbar-light bg-light">
	<a class="navbar-brand" href="#">SymfonyShop</a>
	<button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor03" aria-controls="navbarColor03" aria-expanded="false" aria-label="Toggle navigation">
		<span class="navbar-toggler-icon"></span>
	</button>

	<div class="collapse navbar-collapse" id="navbarColor03">
		<ul class="navbar-nav mr-auto">
			<li class="nav-item active">
				<a class="nav-link" href="{{ path('home') }}">Accueil
					<span class="sr-only">(current)</span>
				</a>
			</li>

		</div>
	</li>
</ul>
<ul class="navbar-nav">
	<li class="nav-item">
		<a href="{{ path('cart_show') }}" class="nav-link">
		<i class="fas fa-shopping-cart"> articles {{ cartService.detailCartItem|length }}</i>
		Total :({{ cartService.total | amount }})
	
		</a>
	</li>
	{% if app.user %}
		<li class="nav-item">
			<a href="{{ path('Commande_index')}}" class="nav-link">mes commandes</a>
		</li>
		<li class="nav-item">
			<a href="{{ path('app_logout') }}" class="btn btn-danger">logout</a>
		</li>
	{% else %}
		<li class="nav-item">
			<a href="#" class="nav-link">Inscription</a>
		</li>
		<li class="nav-item">
			<a href="{{ path('app_login') }}" class="btn btn-success">login</a>
		</li>

	{% endif %}
</ul>
</div>
</nav>
